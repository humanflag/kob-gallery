{
  "name": "kob-gallery-root",
  "version": "1.0.0",
  "description": "Kling & Bang Gallery Archive",
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "start": "cd website && npm start",
    "dev": "cd website && npm run dev"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/humanflag/kob-gallery.git"
  }
}